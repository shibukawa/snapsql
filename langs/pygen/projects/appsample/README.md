# SnapSQL Python Sample Tests

This directory hosts a lightweight pytest configuration that exercises the Python code generated from `testdata/appsample`.

## Running the tests

```bash
python -m pip install -r requirements.txt
python -m pytest
```
